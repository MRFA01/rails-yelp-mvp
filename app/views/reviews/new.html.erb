<h1>Add a new review</h1>

<%= simple_form_for [@restaurant, @review] do |form| %>
  <%= form.input :content, label: "add your review" %>
  <%= form.input :rating, collection: 1..5, label: "add your rating" %>
  <%= form.button :submit %>
<% end %>
